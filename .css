:root {
    --soft-bg: #f0f0f5;  /* Soft light background color */
    --gradient-bg: linear-gradient(to bottom, rgba(255, 255, 255, 0.8), rgba(200, 200, 200, 0.9));  /* Soft ethereal gradient */
    --border-light: rgba(255, 255, 255, 0.6);  /* Light border for soft look */
    --highlight-color: rgba(0, 173, 255, 0.8);  /* Light highlight color for accents */
    --shadow-light: rgba(0, 0, 0, 0.1);  /* Light shadows for depth */
    --text-color: #444;  /* Subtle text color */
    --font-family: 'Georgia', serif;  /* Elegant font style */
}

body {
    background-image: url('https://s3.amazonaws.com//depot.pacdudegames.com/spacehey/bliss.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    font-family: var(--font-family);
    color: var(--text-color);
}

body:before {
    content: " ";
    height: 100vh;
    width: 100vw;
    display: block;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    background-image: url('https://media.giphy.com/media/LbSdXQbS0XVVC/giphy.gif');
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center center;
    animation: fadeAnimation 3s ease-out 0s 1 normal forwards;
    pointer-events: none;
}

@keyframes fadeAnimation {
    0% { opacity: 1; }
    75% { opacity: 0.7; }
    100% { opacity: 0; }
}

header {
    background: var(--gradient-bg);
    padding: 2rem;
    text-align: center;
    color: var(--highlight-color);
    border-radius: 15px;
    box-shadow: 0px 4px 10px var(--shadow-light);
    margin-bottom: 2rem;
}

header h1 {
    font-size: 2.5rem;
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
}

header p {
    font-size: 1.2rem;
    margin-top: 0.5rem;
    opacity: 0.8;
}

nav, footer {
    margin: 1em;
    position: relative;
    box-shadow: 5px 8px 10px var(--shadow-light);
}

footer {
    margin-bottom: 3rem;
    border: 2px solid var(--highlight-color);
}

nav .top, nav .links {
    border-left: 2px solid var(--highlight-color);
    border-right: 2px solid var(--highlight-color);
}

main {
    background: none;
    padding: 2rem;
    font-size: 1.2rem;
    line-height: 1.6;
}

.profile-wrapper {
    background: var(--soft-bg);
    padding: 2rem;
    border-radius: 15px;
    box-shadow: 0px 4px 12px var(--shadow-light);
}

.profile h1 {
    color: var(--highlight-color);
    text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
}

.profile .left {
    display: flex;
    flex-direction: column;
    width: 100%;
}

.profile .general-about {
    background: #FFF;
    padding: 1em;
    box-shadow: 5px 8px 10px var(--shadow-light);
    border-radius: 10px;
    border: 1px solid var(--border-light);
}

.profile .left .mood {
    background: #FFF;
    padding: 1em;
    border-radius: 10px;
    box-shadow: 5px 8px 10px var(--shadow-light);
    margin-top: 1em;
}

.profile .left .mood p {
    margin-left: 1em;
}

.profile .left .mood:before {
    content: "🙂 Mood";
    display: block;
    padding: 1em;
    background: var(--gradient-bg);
    font-family: var(--font-family);
    font-size: 1.2rem;
    color: var(--highlight-color);
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    border-radius: 15px;
}

.profile .contact {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: var(--highlight-color);
    color: #FFF;
    padding: 1em;
    text-align: center;
}

.profile .contact a {
    color: #FFF;
    text-decoration: none;
    font-weight: bold;
    padding: 0.5em;
    background: rgba(0, 173, 255, 0.6);
    border-radius: 5px;
    transition: background 0.3s;
}

.profile .contact a:hover {
    background: rgba(0, 173, 255, 1);
}

.music-section {
    margin-top: 2rem;
    background: var(--soft-bg);
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0px 4px 12px var(--shadow-light);
}

.music-section iframe {
    width: 100%;
    height: 200px;
    border-radius: 10px;
}

.music-section h2 {
    text-align: center;
    font-size: 1.8rem;
    margin-bottom: 1rem;
    color: var(--highlight-color);
}
